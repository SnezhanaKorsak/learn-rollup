// Файл, который будет экспортировать все компоненты нашего пакета.
// Именно с этим файлом мы будем работать в дальнейшем — он будет отправной точкой в нашей сборке
// Rollup будет обрабатывать те файлы, которые в нём указаны.

export * from './Card';
